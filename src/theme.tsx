import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Manrope",
  },
  palette: {
    primary: {
      main: "#F97316",
    },
  },
});

export default theme;
